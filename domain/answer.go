package domain

// correctAnswers builds map with questions and corresponding correct answers
func correctAnswers() map[string]string {
	ca := make(map[string]string)
	ca[adgjl] = h2
	ca[bm] = r3
	ca[cbm] = k479
	ca[dgjl] = r3
	return ca
}
